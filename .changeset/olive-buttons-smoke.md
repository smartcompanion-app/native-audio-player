---
'@smartcompanion/native-audio-player': patch
---

Fix the repository and issues URLs in `package.json`, which pointed at a `smartcompanion-io` organisation that does not exist. This corrects the links shown on npm, and the homepage and source in the CocoaPods podspec, which are both derived from these fields.
